    DEVICE_FRAMEWORK_COMPATIBILITY_MATRIX_FILE := device/generic/goldfish/device_framework_matrix.xml
    
    
    # aidl_interface
    PRODUCT_PACKAGES += vendor.eightman.cpu

    # cc_binary
    PRODUCT_PACKAGES += vendor.eightman.cpu-service

    # cc_binary
    PRODUCT_PACKAGES_DEBUG += eightman_cpu_tester

    # android_app
    #PRODUCT_PACKAGES += EightmanServices

    
